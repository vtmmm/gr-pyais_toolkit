id: pyais_toolkit_nmea_bytes_to_strings
label: NMEA Bytes to Strings
category: '[pyais_toolkit]/Utilities'

templates:
  imports: from gnuradio import pyais_toolkit
  make: pyais_toolkit.nmea_bytes_to_strings()

inputs:
- label: nmea_bytes
  domain: message
  dtype: message
  optional: true

outputs:
- label: nmea_list
  domain: message
  dtype: message
  optional: true

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
